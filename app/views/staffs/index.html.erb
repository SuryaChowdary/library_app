<h1> List of all Employees in Library</h1>
<table class="table">
  <thead>
    <tr>
      <th> Name </th>
      <th> Email </th>
      <th> Phonenumber </th>
      <th> Gender </th>
      <th> Age </th>
      <th> Date_of_birth </th>
      <th> Address </th>
      <th colspan="3"> Actions </th>
    </tr>
  </thead>
    <tbody>
      <% @staffs.each do |staff| %>
        <tr>
      <td><%= staff.name %></td>
      <td colspan=1><%= staff.email %></td>
      <td ><%= staff.phonenumber %></td>
      <td><%= staff.gender %></td>
      <td><%= staff.age %></td>
      <td><%= staff.date_of_birth%></td>
      <td><%= staff.address%></td>
      <td><%= link_to 'Show', staff_path(staff), class: 'btn btn-info' %></td>
      <td><%= link_to 'Edit', edit_staff_path(staff), class: 'btn btn-primary' %></td>
      <td><%= link_to 'Delete', staff_path(staff) , method: :delete , data: {confirm: "Are you sure?"}, class: 'btn btn-danger'%></td>
        </tr>
      <% end %>
    </tbody>
</table>

<p>
  <%= link_to 'Add new Employee' , new_staff_path, class: 'btn btn-primary' %>
</p>

<p>
  <%= link_to 'Home' , root_path, class: 'btn btn-warning' %>
</p>