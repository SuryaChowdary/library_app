<h1> List of Locations</h1>

<p><strong> name: </strong> <%= @location.name%></p>

<p> List of all companies of <%= @location.name%> </p>
  <% @location.companies.each do |company| %>
    <table>
      <tr>
        <td><%= company.name %></td>
     </tr>
    </table>
  <% end %>

<p>
  <%= link_to 'Back' , locations_path %>
</p>